{
  "name": "ts_scraper",
  "version": "1.0.0",
  "description": "Get DOM structure for requested HTML page",
  "main": "index.js",
  "scripts": {
    "start": "npx tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node-lts": "^18.12.3",
    "@types/node": "^20.4.5"
  }
}
